{
  "name": "well-nest-functions",
  "version": "0.1.0",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint src --ext .ts"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "deno-bin": "^2.2.7",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.2.5",
    "openai": "^6.44.0",
    "yt-dlp-wrap": "^2.3.12"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/node": "^20",
    "typescript": "^5"
  }
}
